# Set to "true" to encrypt outgoing POST/PUT JSON and FormData bodies for AWS dev API only.
# Also switches API_BASE_URL to AWS dev. Auth endpoints always stay plain JSON.
# Response decryption always runs when the backend returns { encrypted_data }.
EXPO_PUBLIC_ENCRYPTION_ON=false
