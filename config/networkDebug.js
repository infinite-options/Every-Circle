/**
 * Network / Connect debug UI gate.
 * 0 = hide QR·Ably / AsyncStorage debug on Network AND hide the "Debug Mode" row in Settings.
 * 1 = show Settings toggle; sections appear only when the user selects Yes (persisted in AsyncStorage).
 */
export const SHOW_NETWORK_DEBUG_UI = 1;

export const SETTINGS_NETWORK_DEBUG_MODE_KEY = "settings_network_debug_mode";
