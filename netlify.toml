[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback: same rule is also in public/_redirects (copied into dist by expo export).
# Any path (/, /login, /profile, …) serves the app shell so deep links and Apple return URLs work.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

