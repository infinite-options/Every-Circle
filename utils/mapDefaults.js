/** Dummy A — same preset as Settings → Home Address Coordinates. */
export const MAP_PLACEHOLDER_HOME = {
  lat: 37.7893,
  lng: -122.3966,
  label: "Dummy A — Salesforce Park, SF",
};

/** Google Maps–style neighborhood zoom (street-level, slightly closer than before). */
export const DEFAULT_MAP_ZOOM = 17;

/** react-native-maps delta roughly matching DEFAULT_MAP_ZOOM. */
export const DEFAULT_MAP_REGION_DELTA = 0.008;
